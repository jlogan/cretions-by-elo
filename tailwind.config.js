module.exports = {
  content: [
    './**/*.php',           // Scans all PHP files (like index.php)
    './src/**/*.{html,js}'  // Scans your HTML or JS files too if needed
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}